<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field py-4 lg\:font-sans">
    <%= form.label :name, class:"font-bold " %>
    <%= form.text_field :name, id: :user_name, class: "border-2 px-3" %>
  </div>

  <div class="actions py-2">
    <%= form.submit class: "bg-indigo hover:bg-indigo-dark text-white px-2 py-2 rounded font-sans"%>
  </div>
<% end %>
